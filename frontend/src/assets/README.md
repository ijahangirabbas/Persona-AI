Drop persona photos here, named to match the `photo` path set in
`src/data/personas.js`:

- hitesh.jpg
- piyush.jpg

If a photo is missing or fails to load, the app automatically falls
back to a colored circle with the persona's initials, so nothing breaks
if you leave this folder empty.
